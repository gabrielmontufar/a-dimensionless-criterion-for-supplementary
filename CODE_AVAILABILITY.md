# Code Availability

The reproducibility code is included in this package and mirrored at:

```text
https://github.com/gabrielmontufar/a-dimensionless-criterion-for-supplementary
```

Canonical release tag:

```text
demand-polarity-map-v40-submission-reproducible
```

Official commands:

```bash
python scripts/quick_check.py
python scripts/reproduce_core.py
python scripts/reproduce_full.py
```

The quick and core workflows are offline. The core workflow includes the OpenSeesPy 3D solver-backed class-prediction layer. Optional online metadata checks remain outside the evidence needed for the manuscript's main claims.
